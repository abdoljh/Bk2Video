# ── Arabic Book Brief Engine — Phase 1 ────────────────────────────────
# Streamlit Community Cloud reads requirements.txt from the repo root.

# Core UI
streamlit>=1.35.0

# PDF extraction
PyMuPDF>=1.24.0

# Arabic text correction
arabic-reshaper>=3.0.0
python-bidi>=0.6.0

# OCR — scanned PDFs
# Three backends are supported; install whichever you need:
#
#   PaddleOCR (recommended for Arabic — highest accuracy):
#     pip install paddlepaddle paddleocr
#     GPU: pip install paddlepaddle-gpu paddleocr
#
#   EasyOCR (good accuracy, easier install):
easyocr>=1.7.1
#
#   Tesseract (lightweight, needs system tesseract-ocr + language pack):
# pytesseract>=0.3.10
#
# Shared image utilities used by all backends:
Pillow>=10.0.0
numpy>=1.24.0

# Diacritization
# farasapy: local toolkit wrapper — requires Java at runtime; works on
#   machines with Java installed (local dev). Falls back to mishkal on
#   Streamlit Cloud (no Java).
farasapy>=0.1.1
mishkal
